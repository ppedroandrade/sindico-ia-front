import { EmptyState } from "@/components/empty-state"

export function ConversationsList() {
  return <EmptyState title="Nenhuma conversa registrada" description="As conversas reais do chatbot aparecerão aqui." />
}
